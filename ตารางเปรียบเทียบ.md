# บันทึกผลการเปรียบเทียบและการสังเกต (ผลการทดลองจริง)

### 1. ตารางสรุปผลการเปรียบเทียบระหว่าง REST API และ GraphQL

| หัวข้อเปรียบเทียบ                                      | REST API (`/api/v1/students/:id/full`)                              | GraphQL API (`/graphql`)                                                       |
| :----------------------------------------------------- | :------------------------------------------------------------------ | :----------------------------------------------------------------------------- |
| **จำนวนคำขอ (Requests) สำหรับดูข้อมูลนักศึกษา 1 คน**   | **1 คำขอ** <br>(ได้รับข้อมูลวิชาครบ แต่มีข้อมูลส่วนอื่นปนมาด้วย)    | **1 คำขอ** <br>(ได้เฉพาะข้อมูลตามที่กำหนดใน Query)                             |
| **จำนวนคำขอ (Requests) สำหรับดูข้อมูลนักศึกษาทั้งหมด** | **3 คำขอ** <br>(คำขอหลักดึงนักศึกษา + คำขอดึงรายวิชาแยกทีละวิชา)    | **1 คำขอ** <br>(ดึงข้อมูลโครงสร้างแบบ Nested ครบทุกคนในรอบเดียว)               |
| **ข้อมูลส่วนเกินที่เกิดจาก Over-fetching**             | **พบฟิลด์เกินความจำเป็น 3 ฟิลด์:**<br>`email`, `phone`, `courseIds` | **ไม่พบข้อมูลส่วนเกิน** <br>(ระบบส่งกลับมาเฉพาะฟิลด์ที่เลือกพิมพ์สั่งใน Query) |

---
