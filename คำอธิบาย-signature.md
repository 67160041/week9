# เหตุใด token ที่ถูกแก้ไขแม้เพียงตัวอักษรเดียวจึงถูกปฏิเสธ

## สรุปหลักการ Signature Integrity

โครงสร้างของ JSON Web Token (JWT) ประกอบด้วย 3 ส่วนหลักที่เชื่อมต่อกันด้วยจุด ได้แก่ `Header.Payload.Signature` โดยในขั้นตอนการออก Token เซิร์ฟเวอร์จะนำข้อมูล `Header` และ `Payload` มารวมกัน แล้วนำไปคำนวณผ่านฟังก์ชันแฮช (Cryptographic Hash Function) ร่วมกับรหัสลับประจำเซิร์ฟเวอร์ที่เรียกว่า **`JWT_SECRET`** เพื่อสร้างเป็น `Signature` ดิจิทัลแนบไปกับ Token

### ปัจจัยสำคัญที่ทำให้ Token ถูกปฏิเสธเมื่อถูกแก้ไขแม้เพียงตัวอักษรเดียว:

1. **คุณสมบัติ Avalanche Effect**  
   ฟังก์ชันแฮชทางเข้ารหัสวิทยามีคุณสมบัติพิเศษคือ หากข้อมูลอินพุตในส่วน `Header` หรือ `Payload` มีการเปลี่ยนแปลงไปเพียงอักขระเดียว ผลลัพธ์ของค่า Signature ที่คำนวณได้ใหม่จะเปลี่ยนแปลงไปอย่างสิ้นเชิงแบบคาดเดาไม่ได้

2. **กระบวนการ Verification ฝั่ง Server**  
   เมื่อเซิร์ฟเวอร์ได้รับ Token จาก Request จะนำ `Header` และ `Payload` ของ Token นั้นมาคำนวณ Signature ใหม่อีกครั้งด้วย `JWT_SECRET` ตัวเดิม

3. **การตรวจพบ Signature Mismatch**  
   เซิร์ฟเวอร์จะนำ Signature ที่คำนวณได้ใหม่ไปเปรียบเทียบกับ Signature ที่แนบมาท้าย Token หากมีการดัดแปลงตัวอักษรใน Token ค่า Signature สองชุดนี้จะไม่ตรงกัน (`Signature Mismatch`)

---

> **สรุป:** เซิร์ฟเวอร์จะรู้ทันทีว่า Token ผ่านการปลอมแปลงข้อมูล และไม่ได้มาจากเซิร์ฟเวอร์ที่ถือ `JWT_SECRET` ตัวจริง ระบบจึงปฏิเสธการเข้าถึงทันทีด้วย \*\*Status Code 401
