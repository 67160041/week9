# บันทึกผลการทดสอบ Attack Scenario (สัปดาห์ที่ 4)

## 1. ทดสอบ Payload ขนาดใหญ่ (ขั้นตอนที่ 3.4)

- **ลองทดสอบ:** ดักขนาดไฟล์ใน `index.js` ไว้ที่ `10kb` ด้วยคำสั่ง `express.json({ limit: "10kb" })` แล้วลองใช้ Postman พิมพ์ข้อความมั่ว ๆ ใน Body ยาวเกิน 10 KB ส่งไปที่ `POST /api/v1/students`
- **ผลที่ได้:** Server ไม่ยอมให้ผ่าน ตอบกลับมาเป็น **HTTP Status 413 Payload Too Large** พร้อมโชว์หน้า Error ตามทรงที่เราเซ็ตไว้
- **ข้อสังเกต:** ถ้าเราไม่ดักไว้ตรงนี้ ถ้ามีคนหมั่นไส้ส่งไฟล์หรือข้อความใหญ่เป็น Gigabyte เข้ามาพร้อมกัน Server เราจะใช้ RAM จนหมด แล้วระบบก็จะล่มทันที การตั้ง limit ไว้เลยช่วยเซฟ Server ไม่ให้ล่มเพราะโดนยิงก่อกวน (DoS Attack)

---

## 2. ทดสอบ Data ผิดประเภท หรือ Type Confusion (ขั้นตอนที่ 3.5)

- **ลองทดสอบ:** ลองใส่ JSON ที่ส่งค่าผิดประเภท เช่น ปกติช่อง `name` ต้องส่งเป็นข้อความ (String) แต่เราลองส่งเป็น Array `["สมชาย"]` หรือ Object `{ "$ne": null }` เข้าไปแทน
- **ผลที่ได้:** ตัวโค้ดตรง Validation ที่เราเช็ค `typeof` ไว้ดักจับได้ทันที แล้วตอบกลับมาเป็น **HTTP Status 400 Bad Request**
- **ข้อสังเกต:** ใน JS ถ้าเราไม่เช็คชนิดข้อมูลให้ดี แล้วโค้ดเราดันไปสั่งกดตัดคำหรือแปลงข้อความ เช่น `name.trim()` มันจะขึ้นบั๊ก `TypeError` แถมการปล่อยให้ส่ง Object เข้ามาได้ มักจะถูกพวกแฮกเกอร์ใช้เป็นช่องโหว่แอบฉีดคำสั่งแปลก ๆ เข้ามาขโมยข้อมูลใน Database (NoSQL Injection) ได้

---

## 3. สรุปคือ

การใส่ Middleware ดักขนาดข้อมูล + เช็คชนิดตัวแปร + ทำตัวจัดการ Error รวมไว้ที่เดียว ช่วยให้ API ของเราปลอดภัยขึ้นเยอะ เวลาเจอคนส่งข้อมูลมั่วๆ หรือพยายามก่อกวน ระบบก็ยังทำงานต่อได้ ไม่ล่มง่ายๆ
